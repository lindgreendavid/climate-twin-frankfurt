# Contributing

Open an issue before changing stations, periods, exclusions or inference. Submit focused changes
with tests and documentation; run lint, format, typing, tests, package build, live DWD regeneration,
registry comparison and the full site suite. Never rewrite the frozen v0.1 registry without a new
versioned protocol and review. Preserve provenance, attribution, uncertainty and limitations.
